Create a professional AI-driven application named 'Woxsom Code'. This application is a sophisticated coding assistant platform designed to build complex AI systems, platforms, and SaaS products based on user prompts.

1. Core Architecture (Multi-Agent System):
The application must implement a multi-agent orchestration pattern using 5 provided Groq API keys. Organize the logic into three distinct groups:

Group A (Planner - 1 API key): Takes the user's initial prompt, decomposes it into a project structure (Trello-style tasks), selects the optimal tech stack, and defines the roadmap.

Group B (Executors - 2 API keys): Executes the tasks in parallel. One agent focuses on Frontend/UI, the other on Backend/AI logic. They must use the most capable models from the provided list (e.g., llama-3.3-70b-versatile or qwen/qwen3-32b).

Group C (Critic/Optimizer - 2 API keys): Reviews the code generated by Group B, checks for errors/security issues, optimizes the code, and packages it into a valid file structure.

2. Model Selection Logic:
The app must have a built-in logic to dynamically select the best model from the available 15 models (e.g., llama-3.3-70b-versatile for reasoning, llama-3.1-8b-instant for UI, etc.) based on the specific sub-task complexity.

3. User Interface & Experience:

Design: Modern, minimalist, and sleek. Inspired by Claude Code but with a unique, futuristic aesthetic (use Tailwind CSS, dark mode, glassmorphism).

Functionality: Support multiple chat sessions. Chats must be persisted in a local database (e.g., SQLite or JSON file).

Download System: Once the task is finished, the app must provide a 'Download Project' button that compresses the entire generated file structure into a ZIP folder (including all necessary configurations like .vscode/settings.json for VS Code integration).

4. Implementation Steps for Replit Agent:

Setup: Create an interface to securely accept 5 Groq API keys from the user at startup. Store them in environment variables or a secure configuration file.

Infrastructure: Build the backend using Python (FastAPI/LangChain).

Frontend: Develop the UI in React/Next.js to handle chat interactions, status updates of the agents, and the final download mechanism.

Workflow: Implement the agentic chain logic as defined above.

5. Provided Model List for Logic:
[Include your list of 15 models here: groq/compound, groq/compound-mini, llama-3.1-8b-instant, llama-3.3-70b-versatile, meta-llama/llama-4-scout-17b-16e-i..., meta-llama/llama-prompt-guard-2-2..., meta-llama/llama-prompt-guard-2-8..., openai/gpt-oss-120b, openai/gpt-oss-20b, openai/gpt-oss-safeguard-20b, whisper-large-v3, whisper-large-v3-turbo, qwen/qwen3-32b, canopylabs/orpheus-arabic-saudi, canopylabs/orpheus-v1-english]